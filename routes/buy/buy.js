module.exports = (router, Users, Items)=>{
  // router.post('/', async (req,res)=>{
  //   var basketList = await Users.findOne(req.body)
  //   basketList = basketList.basket;
  //   for (var i =0; basketList[i] != null; i++){
  //     var result1 = Items.findOne({name : basketList[i].name})
  //     result1.rating = result1.rating + 1;
  //     var result2 = Items.update(
  //       {}.
  //       {$set : {} }
  //     )
  //   }
  // })
  return router;
}
